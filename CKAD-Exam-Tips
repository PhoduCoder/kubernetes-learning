Set these aliases

alias k = kubectl 

export do = "==dry-run=client -o=yaml"  

#k create deploy nginx --image=nginx $do 

====
VIM settings 
cat ~/.vimrc

set tabstop=2
set expandtab
set shiftwidth=2

====

Team Sunny needs to identify some of their Pods in namespace sun.
They ask you to add a new label protected: true to all Pods with an 
existing label type: worker or type: runner. 
Also add an annotation protected: do not delete this pod to all
Pods having the new label protected: true
===


